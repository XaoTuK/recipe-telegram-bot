#!/usr/bin/env python3
import os
import logging
from telegram import Update
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes
import requests
import json

# Детальная настройка логирования
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.DEBUG  # Изменили на DEBUG для максимальной детализации
)
logger = logging.getLogger(__name__)

# Получаем конфигурационные переменные из Railway
BOT_TOKEN = os.environ['BOT_TOKEN']
YANDEX_API_KEY = os.environ['YANDEX_API_KEY'] 
FOLDER_ID = os.environ['FOLDER_ID']

# URL для YandexGPT API
YANDEX_GPT_URL = "https://llm.api.cloud.yandex.net/foundationModels/v1/completion"

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик команды /start"""
    welcome_text = """
🍳 Привет! Я RecipeChefAI - твой помощник в готовке!

Просто отправь мне список продуктов через запятую, например:
"курица, рис, лук, морковь, помидоры"

И я придумаю для тебя вкусный рецепт!
    """
    await update.message.reply_text(welcome_text)

async def handle_message(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик текстовых сообщений с продуктами"""
    user_message = update.message.text
    logger.info(f"Получено сообщение от пользователя: {user_message}")
    
    try:
        # Показываем что бот печатает
        await update.message.chat.send_action(action="typing")
        
        # Создаем промт для YandexGPT
        prompt = f"""
Пользователь предоставил следующие продукты: {user_message}

Придумай подробный кулинарный рецепт используя эти продукты. 
Рецепт должен содержать:
1. Название блюда
2. Ингредиенты с количествами
3. Пошаговый способ приготовления
4. Время приготовления

Рецепт должен быть практичным и простым для приготовления в домашних условиях.
        """
        
        logger.debug(f"Создан промт: {prompt}")
        
        # Отправляем запрос к YandexGPT
        recipe = await get_recipe_from_yagpt(prompt)
        
        # Отправляем рецепт пользователю
        logger.info("Успешно сгенерирован рецепт, отправляем пользователю")
        await update.message.reply_text(recipe)
        
    except Exception as e:
        logger.error(f"Ошибка при обработке сообщения: {e}", exc_info=True)
        await update.message.reply_text("😔 Произошла ошибка при генерации рецепта. Попробуйте еще раз.")

async def get_recipe_from_yagpt(prompt: str) -> str:
    """Функция для взаимодействия с YandexGPT API"""
    try:
        logger.info("=== НАЧАЛО ЗАПРОСА К YANDEXGPT ===")
        logger.info(f"FOLDER_ID: {FOLDER_ID}")
        logger.info(f"YANDEX_API_KEY: {YANDEX_API_KEY[:10]}...")  # Логируем только начало ключа
        
        headers = {
            "Content-Type": "application/json",
            "Authorization": f"Api-Key {YANDEX_API_KEY}",
            "x-folder-id": FOLDER_ID
        }
        
        data = {
            "modelUri": f"gpt://{FOLDER_ID}/yandexgpt-lite",
            "completionOptions": {
                "stream": False,
                "temperature": 0.7,
                "maxTokens": 2000
            },
            "messages": [
                {
                    "role": "user", 
                    "text": prompt
                }
            ]
        }
        
        logger.debug(f"Заголовки запроса: {headers}")
        logger.debug(f"Данные запроса: {json.dumps(data, ensure_ascii=False, indent=2)}")
        
        logger.info("Отправка POST запроса к YandexGPT...")
        response = requests.post(YANDEX_GPT_URL, headers=headers, json=data, timeout=30)
        
        logger.info(f"Получен ответ. Статус код: {response.status_code}")
        logger.debug(f"Заголовки ответа: {dict(response.headers)}")
        logger.debug(f"Текст ответа: {response.text}")
        
        if response.status_code != 200:
            logger.error(f"Ошибка API! Статус: {response.status_code}")
            logger.error(f"Тело ошибки: {response.text}")
            
            # Детальный анализ ошибок
            if response.status_code == 400:
                logger.error("Ошибка 400: Неверный запрос. Проверьте FOLDER_ID и данные запроса")
            elif response.status_code == 401:
                logger.error("Ошибка 401: Неавторизован. Проверьте YANDEX_API_KEY")
            elif response.status_code == 403:
                logger.error("Ошибка 403: Доступ запрещен. Проверьте права доступа FOLDER_ID")
            elif response.status_code == 404:
                logger.error("Ошибка 404: Ресурс не найден. Проверьте URL и FOLDER_ID")
            elif response.status_code == 429:
                logger.error("Ошибка 429: Слишком много запросов. Лимит превышен")
            elif response.status_code == 500:
                logger.error("Ошибка 500: Внутренняя ошибка сервера Yandex")
            else:
                logger.error(f"Неизвестная ошибка HTTP: {response.status_code}")
            
            raise Exception(f"API error {response.status_code}: {response.text}")
        
        # Парсим успешный ответ
        result = response.json()
        logger.debug(f"Полный ответ JSON: {json.dumps(result, ensure_ascii=False, indent=2)}")
        
        # Проверяем структуру ответа
        if 'result' not in result:
            logger.error(f"В ответе отсутствует 'result': {result}")
            raise Exception("Неверная структура ответа: нет 'result'")
            
        if 'alternatives' not in result['result']:
            logger.error(f"В result отсутствуют 'alternatives': {result['result']}")
            raise Exception("Неверная структура ответа: нет 'alternatives'")
            
        if not result['result']['alternatives']:
            logger.error("Пустой массив alternatives")
            raise Exception("Пустой ответ от модели")
            
        recipe_text = result['result']['alternatives'][0]['message']['text']
        logger.info(f"Успешно получен рецепт, длина: {len(recipe_text)} символов")
        logger.debug(f"Текст рецепта: {recipe_text}")
        
        return recipe_text
        
    except requests.exceptions.RequestException as e:
        logger.error(f"Ошибка сети при запросе к YandexGPT: {e}", exc_info=True)
        raise Exception(f"Ошибка подключения к YandexGPT: {e}")
    except KeyError as e:
        logger.error(f"Ошибка ключа в структуре ответа: {e}", exc_info=True)
        logger.error(f"Полная структура ответа: {result if 'result' in locals() else 'N/A'}")
        raise Exception(f"Неверный формат ответа от YandexGPT: {e}")
    except json.JSONDecodeError as e:
        logger.error(f"Ошибка парсинга JSON: {e}")
        logger.error(f"Ответ сервера: {response.text if 'response' in locals() else 'N/A'}")
        raise Exception("Неверный JSON в ответе")
    except Exception as e:
        logger.error(f"Неожиданная ошибка в get_recipe_from_yagpt: {e}", exc_info=True)
        raise

def main():
    """Основная функция запуска бота"""
    try:
        logger.info("=== ЗАПУСК БОТА ===")
        
        # Детальная проверка переменных
        logger.info("Проверка переменных окружения:")
        logger.info(f"BOT_TOKEN: {'✅ установлен' if BOT_TOKEN else '❌ НЕ УСТАНОВЛЕН'}")
        logger.info(f"YANDEX_API_KEY: {'✅ установлен' if YANDEX_API_KEY else '❌ НЕ УСТАНОВЛЕН'}")
        logger.info(f"FOLDER_ID: {'✅ ' + FOLDER_ID if FOLDER_ID else '❌ НЕ УСТАНОВЛЕН'}")
        
        if not all([BOT_TOKEN, YANDEX_API_KEY, FOLDER_ID]):
            logger.error("❌ Не все переменные окружения установлены!")
            return
        
        # Создаем приложение
        application = Application.builder().token(BOT_TOKEN).build()
        
        # Добавляем обработчики
        application.add_handler(CommandHandler("start", start))
        application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message))
        
        # Запускаем бота
        logger.info("✅ Бот запускается...")
        application.run_polling(
            drop_pending_updates=True,
            allowed_updates=["message"]
        )
        
    except Exception as e:
        logger.error(f"❌ Критическая ошибка при запуске бота: {e}", exc_info=True)

if __name__ == "__main__":
    main()
